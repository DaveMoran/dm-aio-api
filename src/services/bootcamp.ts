// Phase 1 stub — no DB calls yet.
// Phase 2: inject Supabase/Drizzle client here.

export const bootcampService = {
  getAllCompletions: async () => {
    return []
  },

  getAllContent: async () => {
    return []
  },

  toggleCompletion: async (_itemKey: string, _complete: boolean) => {
    return null
  },

  saveContent: async (_itemKey: string, _content: string) => {
    return null
  },
}
